{
  "name": "kueri",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"make dev-api\" \"make dev-web\"",
    "dev:api": "make dev-api",
    "dev:web": "make dev-web",
    "install:all": "make install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
