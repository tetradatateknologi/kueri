# App logo

Place your application logo here as **`logo.png`**.

It is loaded automatically by `KueriLogo` (`/logo.png`) in the sidebar, welcome screen, and settings.

Recommended: square PNG with transparent background, at least 512×512 px.

Until `logo.png` exists, the app shows a gradient fallback mark.
