# Empty = use same origin (Vite dev proxy forwards /health and /ping to the API).
# Set explicitly when calling the API without the dev proxy (e.g. production build).
# VITE_API_BASE_URL=http://localhost:8080
